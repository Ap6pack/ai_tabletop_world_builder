services:
  api:
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      API_RELOAD: "true"

  frontend:
    volumes:
      - ./app:/app/app
